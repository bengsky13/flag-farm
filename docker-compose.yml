services:
  destructive-farm:
    build: server
    container_name: farmer
    ports:
      - "5002:5002"
    restart: unless-stopped
  # attack-map:
  #   build: attack-map
  #   container_name: attack-map
  #   environment:
  #     - WEBSOCKET_URI=ws://localhost:5002
  #     - DISCORD_WEBHOOK_URL=https://discord.com
  #     - TEAM_IDENTIFIER="PETIR - tolong dipakai dulu sepatunya"
  #   restart: unless-stopped
  # pkappa2:
  #   build:
  #     context: pkappa2
  #   ports:
  #     - 8080:8080
  #   restart: unless-stopped
  #   env_file:
  #     # Change values in .env file
  #     - pkappa2/.env
